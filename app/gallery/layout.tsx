import Passthru from '@/features/core/Passthru'
import { Metadata } from 'next'
export default Passthru

export const metadata: Metadata = {
	title: 'Gallery',
	description: 'A very haphazard collection of some of my photographs',
}
